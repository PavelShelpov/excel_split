1. Условное форматирование как не переносилось, так и не переносится.
    В исходном файле для диапазона $K$5:$K$2059, если Да, то заливка зеленым, если Нет или Запланирован - красный.
    В итоговых файлах условного форматирование нет вообще
2. Не реализован выбор всех комбинаций категорий, требуемый порядок:
    1. Выбирается файл
    2. Выбирается папка
    3. Выбираются колонки для фильтрации:
        Например, есть колонки с категориями:
            '1': '1.1', '1.2', '1.3'
            '2': '2.1', '2.2'
            '3': '3.1', '3.2'
            '4': '4.1', '4.2'
            '5': '5.1'
            Выбираются колонки '1', '4' и '5'
    4. Из колонки '1' предлагается выбрать категории, например, выбирается '1.1' (это будет первый результирующий файл) и '1.2' (это второй)
    5. Для категорий '1.1' и '1.2' (или всех категорий, если их выбрано больше) предлагается выбор:
        1. Применить ли колонку '4' полность (все категории) к колонке '1' (ко всем выбранным категориям)?:
            ВАЖНО! применение всех категорий не нужно разбивать на шаги по каждой из категорий
            Current filters: 
                - '1': '1.1', '1.2'
                Apply all categories of '4' for them (y/n)?
            Если y
                Тогда результирующие фильтры на этом шаге: '1.1', '1.2', '1.1+4.1', '1.1+4.2', '1.2+4.1', '1.2+4.2'
        2. Если пользователь выбирает точеный выбор:
            1. Предлагаются доступные для фильтра '1.1' категории из колонки '4', например выбирается '4.1'
            2. Предлагаются доступные для фильтра '1.2' категории из колонки '4', например не выбирается ничего
            3. Тогда результирующие фильтры на этом шаге: '1.1', '1.2', '1.1+4.1', '1.2+4.1'
    6. Для категорий попадающих в фильтры '1.1+4.1' и '1.2+4.1':
        1. Применить ли колонку '5' полность (все категории) к результатам предыдущих фильтров?:
            1. Здесь не зависимо от выбора попадает категория 5.1, тогда результирующие фильтры:
                '1.1', '1.2', '1.1+4.1', '1.2+4.1', '1.1+4.1+5.1', '1.2+4.1+5.1'
    7. Фильтрация по всем выбранным комбинациям и сохранение результатов